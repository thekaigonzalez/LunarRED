# actually hello world this time

@M:
# actually hello world this time
  mov R1, 0x48 
  mov R1, 0x45
  mov R1, 0x4c
  mov R1, 0x4c
  mov R1, 0x4f
  mov R1, 0x20
  mov R1, 0x57
  mov R1, 0x4f
  mov R1, 0x52
  mov R1, 0x4c
  mov R1, 0x44
  mov R1, 0x21
  mov R1, 0x0a
  each R1
  halt
